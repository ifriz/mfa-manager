# MFA Manager Environment Configuration
# Copy this file to .env and modify as needed

# Flask Configuration
FLASK_ENV=production

# Security
# Generate a secure secret key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-very-secure-secret-key-here

# Database Configuration
DATABASE_PATH=/app/data/mfa_manager.db

# Application Port Configuration
# Uncomment and set to change the port (default: 4570)
PORT=4570

# Alternative: Flask-specific port variable (PORT takes precedence)
# FLASK_PORT=4570

# Host Configuration (optional)
# HOST=127.0.0.1  # For development
# HOST=0.0.0.0    # For production/Docker
